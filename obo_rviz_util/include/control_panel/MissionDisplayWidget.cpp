#include "MissionDisplayWidget.h"

MissionDisplayWidget::MissionDisplayWidget(QWidget *parent, std::shared_ptr<rclcpp::Node> node)
    : QWidget(parent), node_(node) {
    // Constructor implementation remains the same
    // ...
}

// Destructor and other methods remain unchanged
// ...
